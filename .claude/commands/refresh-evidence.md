---
description: Review the active workspace evidence bank for consistency and unsupported claims
argument-hint: (optional) evidence filename
---

Resolve the active workspace and inspect the requested dossier, or every dossier when no argument is supplied. Compare each claim with the candidate-provided source material available in the private workspace. Propose explicit diffs for drift, unsupported scope, stale status, or missing outcomes.

Do not read external private notes unless the user supplied or authorized them. Do not write evidence changes without confirmation. Preserve the truthfulness and sanitization rules in `CLAUDE.md`.
