---
description: Build and verify one CV variant from the active workspace
argument-hint: <variant slug>
---

Run `node build.mjs $ARGUMENTS` from the repository root. If the one-pager overflows, apply the cut order in `CLAUDE.md`, rebuild, and stop only when the build succeeds. Report the generated files plus bullet-lint and keyword-coverage findings without turning unsupported keywords into claims.
